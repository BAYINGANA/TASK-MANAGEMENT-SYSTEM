import services.*;
import utils.ConsoleMenu;
import models.*;

public class Main {
    public static void main(String[] args) {
        ConsoleMenu menu = new ConsoleMenu();
        menu.MainMenu();
    }
}
